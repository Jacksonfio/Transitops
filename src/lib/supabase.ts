// Supabase has been removed from this codebase.
// This file remains only to avoid build-time import failures if any component still references it.
// It intentionally provides a non-functional stub.

export const supabase = null as any;
export function isSupabaseConfigured() {
  return false;
}

